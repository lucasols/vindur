{
  "scripts": {
    "test": "pnpm -r run test",
    "test:watch": "pnpm --filter vindur run test:watch",
    "test:ui": "pnpm --filter vindur run test:ui",
    "build": "pnpm -r run build",
    "lint-all": "pnpm -r run lint",
    "dev": "pnpm --filter vindur-test-app run dev",
    "lib:build": "pnpm --filter vindur run build",
    "lib:lint": "pnpm --filter vindur run lint",
    "lib:test": "pnpm --filter vindur run test",
    "lib:test:ui": "pnpm --filter vindur run test:ui",
    "vite:test": "pnpm --filter vite run test",
    "e2e:test": "pnpm --filter e2e-tests run test",
    "e2e:show-report": "pnpm --filter e2e-tests run show-report",
    "generate-spec": "node scripts/generate-spec-index.ts",
    "sync-ai": "node scripts/sync-ai.ts",
    "run-app-test": "pnpm --filter vindur-test-app dev"
  },
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
